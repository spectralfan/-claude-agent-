{
  "id": "python-pytest",
  "language": "python",
  "displayName": "Python + pytest (uv)",
  "detectFiles": ["pyproject.toml", "uv.lock", "requirements.txt", "setup.py"],
  "scaffoldTemplate": "python-pytest",
  "skillId": "python-autonomous-dev",
  "suggestedMcpTools": ["execute_command"],
  "verifyWorkflow": "list_stack_verify_commands → run_stack_verify(\"uv run pytest -q\")；失败则读输出修复再测",
  "doneCriteria": "uv run pytest exit 0",
  "suggestedAgentTools": ["execute_command"],
  "verifyCommands": [
    { "label": "uv sync", "type": "shell", "command": "uv sync" },
    { "label": "uv run pytest -q", "type": "shell", "command": "uv run pytest -q" }
  ]
}
